
apiVersion: "spec.keptn.sh/0.2.0"
kind: "Shipyard"
metadata:
  name: "platform"
spec:
  stages:
    - name: "dev-1"
      sequences:
        - name: "artifact-delivery"
          tasks:
            - name: "promotion"
            - name: "deployment"
              properties:
                deploymentstrategy: "user_managed"
